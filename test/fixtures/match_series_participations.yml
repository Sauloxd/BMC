DEFAULTS: &defaults
  created_at: <%= 5.day.ago.to_fs(:db) %>
  updated_at: <%= 5.day.ago.to_fs(:db) %>

ranking_bmc_msp_tocha:
  <<: *defaults
  match_series: ranking_bmc
  user: tocha

ranking_bmc_msp_saulo:
  <<: *defaults
  match_series: ranking_bmc
  user: saulo

ranking_bmc_msp_tipe:
  <<: *defaults
  match_series: ranking_bmc
  user: tipe

ranking_bmc_msp_peter:
  <<: *defaults
  match_series: ranking_bmc
  user: peter