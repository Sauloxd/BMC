<!DOCTYPE html>

<html>
  <head>
    <title>BreakableToy</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="h-screen max-h-screen">
    <div class="flex flex-row h-screen">
      <div class="flex flex-col p-4 justify-items-center min-w-[200px]">
        <a href="<%= root_path %>" class="text-lg font-bold mb-12">
          BMC
        </a>
        <div class="grow">
          <a href="<%= match_series_index_path %>" class="
            flex flex-row p-2 cursor-pointer items-center hover:bg-indigo-100 transition rounded font-semibold
            <%= 'bg-indigo-100 text-indigo-500' if current_page?(match_series_index_path) %>
          ">
            <div class="mr-2">
              🎾
            </div>
            <div class="text-sm">
              Ranking
            </div>
          </a>

          <a class="
            flex flex-row p-2 cursor-pointer items-center hover:bg-indigo-100 transition rounded font-semibold
          ">
            <div class="mr-2">
              📸
            </div>
            <div class="text-sm">
              Photos
            </div>
          </a>

          <a class="flex flex-row p-2 cursor-pointer items-center hover:bg-indigo-100 transition rounded font-semibold">
            <div class="mr-2">
              🏖️
            </div>
            <div class="text-sm">
              Trips
            </div>
          </a>
        </div>
        <%= link_to "Sign out🚪🚶‍♀️", destroy_user_session_path, class: "text-sm", data: { turbo_method: :delete, turbo_confirm: "Are you sure?" } %> 
      </div>
      <div class="grow flex flex-col overflow-auto">
        <div class="flex justify-between items-center px-4 py-2">
          <p class="text-sm font-semibold">
            Hello <a class="hover:text-indigo-500 rounded cursor-pointer"> <%= current_user.email %></a> 👋
          </p>
          <%= link_to edit_user_registration_path, class: "cursor-pointer" do %> 
            <div class="relative overflow-hidden cursor-pointer w-8 h-8 bg-slate-200 block rounded-full">
              <img
                class="w-full"
                src="<%= url_for(current_user.avatar) %>" 
                alt="Your profile picture">
            </div>
          <% end %>
        </div>
        <div class="grow bg-slate-100 overflow-auto">
          <%= yield %>
        </div>
      </div>
    </div>
    
    <% if false %>
      <p class="absolute rounded text-white bg-gray-500 top-2 left-2 p-2 text-sm"><%= notice %></p>
    <% end %>

    <% if alert %>
      <p class="absolute rounded text-white bg-indigo-500 bottom-2 left-2 p-2 text-sm"><%= alert %></p>
    <% end %>

    <div class="modal micromodal-slide" id="form-modal" aria-hidden="true" data-controller="modal"
      data-action="turbo:frame-load->modal#open turbo:submit-end->modal#close"
      >
      <div class="modal__overlay" tabindex="-1">
        <div class="modal__container" role="dialog" aria-modal="true" aria-labelledby="form-modal-title">
          <%= turbo_frame_tag "modal" %>
        </div>
      </div>
    </div>
  </body>
</html>